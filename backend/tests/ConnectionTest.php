<?php
use PHPUnit\Framework\TestCase;

// Simple test to see if backend testing works
class ConnectionTest extends TestCase {
    public function test_phpunit_is_running() {
        $this->assertTrue(true);
    }
}